<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">org.h2.Driver</property>
        <property name="hibernate.connection.url">jdbc:h2:mem:networkgame_${hibernate.connection.url.suffix:default};DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE</property>
        <property name="hibernate.connection.username">sa</property>
        <property name="hibernate.connection.password"></property>

        <!-- Connection pool settings (using default connection provider) -->

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Auto-create/update database schema -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Disable second-level cache for now (requires additional dependencies) -->
        <property name="hibernate.cache.use_second_level_cache">false</property>
        <property name="hibernate.cache.use_query_cache">false</property>

        <!-- Mapping files -->
        <mapping class="database.entity.UserProfileEntity"/>
        <mapping class="database.entity.ScoreRecordEntity"/>
        <mapping class="database.entity.LevelRecordEntity"/>
        <mapping class="database.entity.PlayerRecordEntity"/>
        <mapping class="database.entity.MultiplayerSessionEntity"/>
        <mapping class="database.entity.GameSessionEntity"/>
        <mapping class="database.entity.SystemEntity"/>
        <mapping class="database.entity.WireConnectionEntity"/>
        <mapping class="database.entity.PortEntity"/>

    </session-factory>
</hibernate-configuration>
